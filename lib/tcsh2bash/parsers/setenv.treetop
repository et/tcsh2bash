module Tcsh2Bash
  grammar Setenv
    include BasicTokens

    rule setenv_expression
      'setenv' space assignment {
        def to_bash
        'export ' + assignment.to_bash
        end
      }
    end

  end
end
